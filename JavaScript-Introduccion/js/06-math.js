// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); 
resultado = Math.ceil(2.2); // siempre hacia arriba
resultado = Math.floor(2.9); // siempre hacia abajo
resultado = Math.sqrt(144) // raiz cuadrada
resultado = Math.abs(-200) // negativo pasa a positivo
resultado = Math.min(3, 5, 1, 8) // el mas chico
resultado = Math.max(3, 5, 1, 8) // el mas alto
resultado = Math.random() // numero random
resultado = Math.floor(Math.random() * 30)



console.log(resultado);